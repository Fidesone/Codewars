//We need a function that can transform a number into a string.

console.log(convert_to_string(5));

function convert_to_string(num){
    return num.toString(); 
    
    // convierte un número en una cadena de texto
    // toString();
}